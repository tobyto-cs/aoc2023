// use std::path::{Path, PathBuf};
// use project_root::get_project_root;

// fn get_resource(p: &Path) -> Option<PathBuf> {
//     match get_project_root() {
//         Ok(pr) => {
//             return Some(PathBuf::from(pr).join(p));
//         },
//         Err(_) => None
//     }
// }

// // fn read_txt(p: &Path) -> &'static str {
// //     match get_resource(Path::new(""))
// // }
